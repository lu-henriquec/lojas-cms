import React from 'react';
import styled from 'styled-components';

export const TitleStyle = styled.h1`
	font-size: 22px;
  font-weight: 600;
  text-align: center;
`;

const Title = () => (
  <TitleStyle>CMS - Lojas</TitleStyle>
);

export default Title;
